version: "3.9"

services:
  app:
    build: .
    container_name: job-app-assistant
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
